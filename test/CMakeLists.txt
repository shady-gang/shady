list(APPEND BASIC_TESTS test/empty)
list(APPEND BASIC_TESTS test/basic_blocks1)
list(APPEND BASIC_TESTS test/control_flow1)
list(APPEND BASIC_TESTS test/control_flow2)
list(APPEND BASIC_TESTS test/functions1)
list(APPEND BASIC_TESTS test/identity)
list(APPEND BASIC_TESTS test/memory1)
list(APPEND BASIC_TESTS test/memory2)
list(APPEND BASIC_TESTS test/rec_pow)
list(APPEND BASIC_TESTS test/rec_pow2)
list(APPEND BASIC_TESTS test/restructure1)
list(APPEND BASIC_TESTS test/restructure2)
list(APPEND BASIC_TESTS test/simplify_control)
list(APPEND BASIC_TESTS test/float)
list(APPEND BASIC_TESTS test/fn_decl)

list(APPEND BASIC_TESTS samples/fib)
list(APPEND BASIC_TESTS samples/hello_world)

foreach(T IN LISTS BASIC_TESTS)
    add_test(NAME ${T} COMMAND slim ${PROJECT_SOURCE_DIR}/${T}.slim -o test.spv)
endforeach()
