list(APPEND BASIC_TESTS empty)
list(APPEND BASIC_TESTS basic_blocks1)
list(APPEND BASIC_TESTS control_flow1)
list(APPEND BASIC_TESTS control_flow2)
list(APPEND BASIC_TESTS functions1)
list(APPEND BASIC_TESTS functions2)
list(APPEND BASIC_TESTS identity)
list(APPEND BASIC_TESTS memory1)
list(APPEND BASIC_TESTS memory2)
list(APPEND BASIC_TESTS rec_pow)
list(APPEND BASIC_TESTS rec_pow2)
list(APPEND BASIC_TESTS restructure1)
list(APPEND BASIC_TESTS restructure2)
list(APPEND BASIC_TESTS simplify_control)

foreach(T IN LISTS BASIC_TESTS)
    add_test(NAME ${T} COMMAND slim ${CMAKE_CURRENT_SOURCE_DIR}/${T}.slim -o test.spv)
endforeach()