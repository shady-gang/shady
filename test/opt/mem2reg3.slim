@Exported
fn f varying f32() {
  var i32 i = 1116340224; // 69.0f
  return (*(bitcast[ptr private f32](&i)));
}

@Exported
fn g varying i32() {
  var i32 i = 420;
  return (*(generic_ptr_cast(&i)));
}

@Exported
fn h varying f32() {
  var i32 i = 1116340224; // 69.0f
  val p = &i;
  val p1 = (bitcast[ptr private f32](&i));
  val p2 = generic_ptr_cast(p1);
  return (*p2);
}
