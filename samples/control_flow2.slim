fn fac varying int(varying int count) {
  let x = loop int (varying int i = 1, varying int a = 1) {
    let r = lt i count; // if i < count
    if (r) {
      let i2 = add i 1;
      let a2 = mul i a;
      continue i2 a2;
    } else {
      break a;
    }
    unreachable;
  }
  return x;
}
