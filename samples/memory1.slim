extern i32 extern_int;

fn read_from_extern i32() {
  let loaded = load(extern_int);
  return(loaded);
}

fn read_from_global_ptr i32(uniform ptr global i32 global_ptr) {
  let loaded = load(global_ptr);
  return(loaded);
}
